
import 'package:flutter/material.dart';

class Welcome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Material(
        color: Colors.black,
        child: new Center(
            child: new Text(
              "GAY= BAIVAB ",
              textDirection: TextDirection.ltr,
              style: new TextStyle(fontSize: 30,color: Colors.white, fontStyle: FontStyle.italic),
            )));

    return null;
  }
}
